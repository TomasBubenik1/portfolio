import { useState } from "react";

export function Navbar() {
  const [isNavExpanded, changeNavExpanded] = useState<boolean>(false);
  return (
    <div className=" w-20 h-60 bg-transparent rounded-full border border-slate-400">
    
    </div>
  );
}
